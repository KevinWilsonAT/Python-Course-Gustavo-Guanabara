r1 = float(input('Insert the first rank: '))
r2 = float(input('Insert the second rank: '))
avg = (r1 + r2) / 2

print('The average between the ranks {:.1f} and {:.1f} is: {:.1f}'.format(r1, r2, avg))
