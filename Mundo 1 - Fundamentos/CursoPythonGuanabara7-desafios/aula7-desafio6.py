n = int(input('Insert an integer number: '))
doub = n*2
tri = n*3
sroot = pow(n, (1/2))

print('The integer number is {}, its double is {}, its triple is {} and your square root is {:.2f}'
      .format(n, doub, tri, sroot))
